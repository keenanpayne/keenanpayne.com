{
  "name": "keenanpayne",
  "private": true,
  "version": "1.0.0",
  "description": "The personal website for Keenan Payne.",
  "homepage": "http://keenanpayne.com",
  "devDependencies": {
    "autoprefixer": "^6.0.3",
    "cssmin": "^0.4.3",
    "node-sass": "^3.4.0",
    "postcss-cli": "^2.3.1",
    "watch": "^0.16.0"
  },
  "scripts": {
    "build": "node-sass scss/concise.scss css/site.css | postcss --use autoprefixer < css/site.css -b \"last 2 versions\" | cssmin > css/site.min.css",
    "build:watch": "watch \"npm run build\" scss"
  }
}
